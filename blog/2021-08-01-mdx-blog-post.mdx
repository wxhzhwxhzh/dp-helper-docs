---
slug: mdx-blog-post
title: MDX 日志示例
authors: [slorber]
tags: [docusaurus]
---

日志支持 [Docusaurus 的 Markdown 特性](https://docusaurus.io/docs/markdown-features)，例如 [MDX](https://mdxjs.com/)。

:::tip

用 React 的能力做出可交互的日志。

:::

{/* truncate */}

例如，用 JSX 创建一个可交互按钮：

```js
<button onClick={() => alert('按钮被点击了！')}>点我！</button>
```

<button onClick={() =>document.querySelector('.toggleButton_gllP').click()   }>点击改变主题</button>
